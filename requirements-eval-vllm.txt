# requirements-eval-vllm.txt   [已废弃 / DEPRECATED]
#
# ⚠️ 本文件已废弃，请改用 requirements-eval-vllm-cu121.txt。
#
# 废弃原因:
#   本文件原锁定 vllm==0.8.5.post1 + lm_eval==0.4.9.2，但这要求升级 limo_sft 训练环境
#   的 torch / transformers / lm-eval，会破坏已完成训练的环境。
#
# 当前正确做法（不修改 limo_sft）:
#   conda create -n limo_eval_vllm --clone limo_sft -y
#   conda activate limo_eval_vllm
#   python -m pip install "vllm==0.6.6.post1"
#   python -m pip install -r requirements-eval-vllm-cu121.txt
#
# 目标版本组合（与 limo_sft 一致）:
#   torch==2.5.1+cu121
#   transformers==4.46.3
#   lm-eval==0.4.5
#   vllm==0.6.6.post1
#   peft==0.13.2
#   accelerate==1.1.1
