# requirements.txt —— 训练环境依赖（QLoRA 微调）
# ⚠️ 评测请勿使用本文件。vLLM 评测需要单独环境，见 requirements-eval-vllm-cu121.txt
# 评测环境安装（基于训练环境克隆，不破坏训练环境）:
#   conda create -n limo_eval_vllm --clone limo_sft -y
#   conda activate limo_eval_vllm
#   pip install -r requirements-eval-vllm-cu121.txt
#
# 说明: 评测环境基于训练环境 limo_sft 克隆，仅额外安装 vllm==0.6.6.post1，
# 不升级 torch / transformers / peft / accelerate / lm-eval。
# lm-eval 0.4.5 不含 aime24/aime25/hendrycks_math500，因此使用 eval_tasks/ 本地任务。
# 旧文件 requirements-eval-vllm.txt（vllm 0.8.5.post1 + lm_eval 0.4.9.2）已废弃。
torch>=2.1.0
transformers>=4.43.0
peft>=0.11.0
bitsandbytes>=0.43.0
datasets>=2.20.0
accelerate>=0.33.0
sentencepiece>=0.2.0
protobuf>=4.25.0
numpy>=1.26.0
